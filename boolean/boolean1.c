#include <stdio.h>
#include <stdbool.h> // for using bool type we must include stdbool.h library

int main()
{

    bool x = false;
    if (x == true)
    {
        printf("x is true");
    }
    else
        printf("x is false");
    return 0;
}