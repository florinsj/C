// Learning C
// Florin S

// Bitwise complement (one's complement) operator   ~
#include <stdio.h>
int main()
{

    int a = 5, b = 14;

    // bitwise complement operator (one's complement)  -->   ~
    // 1 become 0 and 0 become 1  ex.  1000  --> one's complement=0111

    printf("\nthe output of the Bitwise complement operator ~a is %d", ~a);

    return 0;
}