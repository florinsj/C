// Learning C
// Florin S

// while  loop  (pre-tested condition)
#include <stdio.h>
int main()
{
    int count = 1;
    while (count < 8)
    {
        printf("Count=%d\n", count);
        count++;
    }
    return 0;
}